# Linux: build against glibc 2.28 (RHEL/Rocky 8), matching the manylinux_2_28
# PyPI wheels. stdlib('c') turns this into a __glibc >=2.28 run requirement.
c_stdlib:
  - sysroot
c_stdlib_version:
  - "2.28"
